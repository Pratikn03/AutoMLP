# Example: passing time budget to a training function
time_budget = 300  # seconds
model.fit(X_train, y_train, time_budget=time_budget)# Example: passing time budget to a training function
time_budget = 300  # seconds
model.fit(X_train, y_train, time_budget=time_budget)